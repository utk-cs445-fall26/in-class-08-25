# Trace Summary

Small command-line tool for summarizing application trace records.

Trace records are stored in `data/events.csv`.

Examples:

```bash
python app.py --user user17
python app.py --date 2026-03-01
```
